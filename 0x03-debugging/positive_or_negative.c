#include "main.h"
/**
 * main - calls other func.
 * positive_or_negative(i) - to know if it is negative or positive
 * i - integer to be tested
 * Return: 0
 */

int main(void)
{
	positive_or_negative(int i);

	return (0);
}
